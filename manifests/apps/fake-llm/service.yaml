apiVersion: v1
kind: Service
metadata:
  name: fake-llm
  labels:
    app.kubernetes.io/name: fake-llm
spec:
  selector:
    app.kubernetes.io/name: fake-llm
  ports:
    - port: 8080
      targetPort: http
      name: http
